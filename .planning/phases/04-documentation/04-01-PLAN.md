---
phase: 04-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: false
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "A technical PM reading only the README knows what Orbit is, how to install it, how to use every skill, and what to ask Engin"
    - "Install section has exactly 3 steps: clone, bash install.sh, /orbit-init"
    - "All 8 skills have a fenced code block usage example"
    - "Engin capabilities table has exactly 7 rows with Ask about and Example query columns"
    - "No spatial vocabulary appears in prose outside fenced code blocks"
    - "No hedging language (can, might, could, help you, allows you to, enables)"
  artifacts:
    - path: "README.md"
      provides: "Complete self-contained project documentation"
      contains: "# Orbit"
      min_lines: 150
  key_links:
    - from: "README.md install section"
      to: "install.sh"
      via: "bash install.sh command"
      pattern: "bash.*install\\.sh"
    - from: "README.md Engin section"
      to: ".claude/agents/engin.md"
      via: "7 capabilities sourced from engin.md"
      pattern: "Strategic Synthesis.*Customer Insight.*RICE.*Decision Tracking.*Meeting Preparation.*Brief.*Contradiction"
---

<objective>
Write the complete README.md that makes Orbit self-explaining and publishable on GitHub. A technical PM landing on the repo knows what it does, how to install it, how to use every skill, and what to ask Engin -- without reading any other file.

Purpose: This is the final phase of Orbit v1.0. The README is the product's front door on GitHub -- it converts browsers into users.
Output: README.md (full replacement of current 10-line placeholder)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation/04-CONTEXT.md
@.planning/phases/04-documentation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete README.md</name>
  <files>README.md</files>
  <read_first>
    README.md
    .planning/phases/04-documentation/04-CONTEXT.md
    .planning/phases/04-documentation/04-RESEARCH.md
    .claude/agents/engin.md
    .claude/skills/orbit-init/SKILL.md
    .claude/skills/orbit-ingest/SKILL.md
    .claude/skills/orbit-status/SKILL.md
    .claude/skills/orbit-brief/SKILL.md
    .claude/skills/orbit-priorities/SKILL.md
    .claude/skills/orbit-decisions/SKILL.md
    .claude/skills/orbit-prep/SKILL.md
    .claude/skills/orbit-artifact/SKILL.md
    install.sh
  </read_first>
  <action>
Replace the 10-line README.md placeholder with a complete, self-contained README following this exact section structure (per D-01, D-03, D-04, D-05):

**Section 1: Hero (per D-01, D-05, D-10)**
- H1: `# Orbit`
- Tagline: "An open-source Claude Code plugin that makes Product Managers 100x productive."
- One-liner scope statement conveying: No code. No infra. No SaaS. Just Claude Code skills and markdown. (exact phrasing is Claude's discretion per CONTEXT.md)
- Brief paragraph: what Orbit IS -- import meeting notes from Granola (meeting notes), Jira (insights/issues), Slack (conversations) per D-02. Auto-organize into themes. Track decisions. Generate briefs, scorecards, PRDs.
- Voice: product-confident per D-10. Direct statements: "Orbit does X." No hedging -- no "can", "might", "could", "help you", "allows you to", "enables".

**Section 2: How It Works**
- Brief explanation of the two-agent model: Astro (auto-organizes notes into themes, extracts action items, logs decisions) and Engin (answers PM questions with cited sources, challenges assumptions)
- Mention the auto-hook: after ingesting a note, Astro organizes it automatically -- no manual step

**Section 3: Install (per D-03)**
- Exactly 3 numbered steps:
  1. Clone: `git clone https://github.com/danieljmedina/orbit.git`
  2. Install (from your project directory): `bash /path/to/orbit/install.sh`
  3. Initialize: `/orbit-init`
- Brief note that install.sh copies skills, agents, settings, and templates into the current project
- Do NOT include Granola setup here -- that goes in its own section per D-03

**Section 4: Skills -- Workflow View (per D-04 layer 1)**
- Three workflow groups showing how skills chain together. Each group has a heading and lists 2-3 skills with a one-sentence description of what happens at that stage:
  - **Get started**: `/orbit-init` (set up workspace), `/orbit-ingest` (import notes from sources)
  - **Daily workflow**: `/orbit-status` (see workspace overview), `/orbit-brief` (morning briefing)
  - **Deep analysis**: `/orbit-priorities` (RICE scoring), `/orbit-decisions` (decision audit), `/orbit-prep` (meeting preparation), `/orbit-artifact` (generate PM documents)
- Each skill mention should briefly say what it does in plain PM language (not spatial vocabulary)

**Section 5: Skills -- Reference Table + Examples (per D-04 layer 2, D-06, D-07)**
- Compact markdown table with columns: Skill | What it does | Example
- One row per skill (8 rows total)
- One-liner descriptions sourced from each SKILL.md `description:` frontmatter field
- Example column shows the invocation command
- After the table, one fenced code block per skill (8 total) showing: the command invocation and a representative output snippet. Source ALL output snippets from the "Code Examples" section of 04-RESEARCH.md -- these are verified against SKILL.md output format sections. Use realistic PM scenarios per D-07 (sprint reviews, stakeholder calls, product decisions -- no generic "your-project" placeholders).

The 8 example blocks (sourced from 04-RESEARCH.md Code Examples section):

1. `/orbit-init` -- show completion output with detected MCPs
2. `/orbit-ingest --granola 7` -- show 4 meetings captured, Astro organizing
3. `/orbit-status` -- show station overview with constellations
4. `/orbit-brief weekly` -- show weekly summary completion
5. `/orbit-priorities launch-readiness` -- show RICE scan completion
6. `/orbit-decisions stalled` -- show coordinate audit with conflict/stalled
7. `/orbit-prep "Q2 Planning" --attendees "Sarah, Mike"` -- show prep completion
8. `/orbit-artifact prd --theme api-redesign` -- show payload delivered

**Section 6: Ask Engin (per D-08, D-09, D-03)**
- Brief intro paragraph mentioning Engin's personality per D-09: "cites sources, challenges assumptions, gives opinionated answers -- not just summaries"
- Compact markdown table per D-08 with columns: Capability | Ask about | Example query
- Exactly 7 rows sourced from engin.md "Your 7 Capabilities" section:
  1. Strategic Synthesis | What to focus on | "What should I focus on this week?"
  2. Customer Insight Analysis | Customer feedback patterns | "What are customers saying about the API?"
  3. RICE Prioritization | Feature prioritization | "Help me prioritize the Q2 backlog"
  4. Decision Tracking | Stalled or conflicting decisions | "What decisions are stalling on the launch?"
  5. Meeting Preparation | Upcoming meeting context | "Prepare for my sprint planning with Sarah and Mike"
  6. Morning/Weekly Brief | Daily or weekly summary | "Give me a weekly brief"
  7. Contradiction & Risk Detection | Hidden risks and conflicts | "Are there any risks I'm missing?"

**Section 7: Granola MCP Setup (per D-03 -- separate optional section)**
- Mark as optional
- Explain: Granola is a macOS meeting notes app; the MCP server connects it to Claude Code
- Keep at "what to configure" level per RESEARCH.md recommendation -- point to official Granola docs rather than exact step-by-step that goes stale
- Note that once configured, `/orbit-ingest` detects Granola automatically

**Section 8: Language note (per D-11)**
- Brief mention: Orbit auto-detects language. "Write in Spanish, get answers in Spanish."

**Section 9: License**
- MIT license line (match existing LICENSE file if present)

**CRITICAL CONSTRAINTS:**
- English only per D-11
- No spatial vocabulary (signals, constellations, missions, coordinates, drifting, payload) in prose OUTSIDE fenced code blocks. Spatial terms are acceptable ONLY inside fenced code blocks showing terminal output.
- No hedging: scan final output for "can", "might", "could", "help you", "allows you to", "enables" and replace with direct statements per D-10
- Source types accurate per D-02: Granola = meeting notes, Jira = insights/issues, Slack = conversations
- All example output derived from 04-RESEARCH.md Code Examples section, NOT invented
  </action>
  <verify>
    <automated>
# Verify README exists and has substantial content
test $(wc -l < README.md) -ge 150 && echo "PASS: line count" || echo "FAIL: too short"

# Verify all 8 skills have examples
for skill in orbit-init orbit-ingest orbit-status orbit-brief orbit-priorities orbit-decisions orbit-prep orbit-artifact; do grep -q "/\$skill" README.md && echo "PASS: $skill present" || echo "FAIL: $skill missing"; done

# Verify Engin 7 capabilities
for cap in "Strategic Synthesis" "Customer Insight" "RICE Prioritization" "Decision Tracking" "Meeting Preparation" "Brief" "Contradiction"; do grep -q "$cap" README.md && echo "PASS: $cap" || echo "FAIL: $cap missing"; done

# Verify install steps (3 steps max before next section)
grep -q "install.sh" README.md && echo "PASS: install.sh referenced" || echo "FAIL: no install.sh"
grep -q "orbit-init" README.md && echo "PASS: orbit-init referenced" || echo "FAIL: no orbit-init"

# Verify no hedging in prose (outside code blocks)
grep -n "can help\|might help\|could help\|allows you to\|enables you" README.md | grep -v '```' | head -5; echo "(empty = PASS)"

# Verify no spatial vocab in prose lines (rough check -- lines not inside code blocks)
grep -n "signals\|constellations\|coordinates\|drifting\|payload" README.md | grep -v '```' | grep -v "^[0-9]*:  " | head -5; echo "(check manually if hits)"
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md exists with 150+ lines
    - README.md contains "# Orbit" as first heading
    - README.md contains "install.sh" in install section
    - README.md contains fenced code block examples for all 8 skills: orbit-init, orbit-ingest, orbit-status, orbit-brief, orbit-priorities, orbit-decisions, orbit-prep, orbit-artifact
    - README.md contains Engin capabilities table with all 7: Strategic Synthesis, Customer Insight Analysis, RICE Prioritization, Decision Tracking, Meeting Preparation, Morning/Weekly Brief, Contradiction and Risk Detection
    - README.md contains "git clone" in install section
    - README.md does NOT contain hedging words ("can help", "might", "allows you to") in prose outside code blocks
    - README.md does NOT contain spatial vocabulary ("signals", "constellations", "coordinates", "drifting") in prose outside code blocks
    - Granola MCP setup is in its own section, not inline with the 3 install steps
  </acceptance_criteria>
  <done>README.md fully written with all sections: hero, how it works, install (3 steps), workflow grouping, reference table + 8 examples, Engin 7-capability table, Granola setup, language note, license</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of README</name>
  <files>README.md</files>
  <action>
Human reviews the completed README.md against all locked decisions (D-01 through D-11). This is a visual and content review checkpoint -- no automated changes.

Review checklist:
1. Hero section: product-first pitch (D-01), accurate source types (D-02), no hedging (D-10)
2. Install: exactly 3 steps (D-03), Granola setup is separate
3. Workflow grouping before reference table (D-04)
4. Scope one-liner present (D-05)
5. Fenced code blocks for examples (D-06), realistic PM scenarios (D-07)
6. Engin table: 7 rows, compact format (D-08), personality mention (D-09)
7. Product-confident voice throughout (D-10)
8. English only, language auto-detect mentioned (D-11)
9. No spatial vocabulary in prose outside code blocks
  </action>
  <verify>Human confirms README meets all criteria or describes issues to fix</verify>
  <acceptance_criteria>
    - Human types "approved" confirming README meets all D-01 through D-11 decisions
    - Or human describes specific issues which are then addressed
  </acceptance_criteria>
  <done>README.md approved by human review against all 11 locked decisions</done>
</task>

</tasks>

<verification>
- README.md has 150+ lines of content
- All 8 `/orbit-*` skills appear with fenced code block examples
- Engin capabilities table has exactly 7 rows
- Install section has exactly 3 numbered steps
- No spatial vocabulary in prose (only inside code blocks)
- No hedging language in prose
- Granola MCP setup is a separate optional section
- Source types accurate: Granola = meeting notes, Jira = insights/issues, Slack = conversations
</verification>

<success_criteria>
1. DOCS-01: README contains install instructions (3 steps), what Orbit is (hero section), Granola setup (separate section), skill reference (workflow grouping + reference table)
2. DOCS-02: README has at least one fenced code block example per skill -- all 8 skills covered
3. DOCS-03: Engin capabilities table has exactly 7 rows with populated "Ask about" and "Example query" columns
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-01-SUMMARY.md`
</output>
