---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orbit-pm/.claude/agents/astro.md
  - orbit-pm/.claude/agents/engin.md
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "astro.md exists with correct YAML frontmatter (name, description, model: sonnet, memory: project, tools, disallowedTools) and the full Astro system prompt"
    - "engin.md exists with correct YAML frontmatter (name, description, model: inherit, memory: project, tools, disallowedTools) and the full Engin system prompt"
    - "Both agents have descriptions that Claude will use for auto-delegation"
  artifacts:
    - path: "orbit-pm/.claude/agents/astro.md"
      provides: "Astro organization agent definition"
      exports: ["name: astro", "model: sonnet", "memory: project"]
    - path: "orbit-pm/.claude/agents/engin.md"
      provides: "Engin PM advisor agent definition"
      exports: ["name: engin", "model: inherit", "memory: project"]
  key_links:
    - from: "orbit-pm/.claude/agents/astro.md"
      to: "orbit-init and orbit-ingest skills"
      via: "auto-delegation from description field"
      pattern: "Delegate to this agent after note ingestion"
    - from: "orbit-pm/.claude/agents/engin.md"
      to: "orbit-brief, orbit-priorities, orbit-decisions, orbit-prep, orbit-artifact skills"
      via: "auto-delegation from description field"
      pattern: "Delegate for any PM question or analysis"
---

<objective>
Create the two agent definition files: astro.md (Organization Agent) and engin.md (Expert PM Advisor). These are complete agent definitions — full YAML frontmatter plus full system prompts from mvp/04-PROMPTS.md.

Purpose: Agents provide persistent memory and specialized intelligence. Phase 2 hooks trigger Astro automatically; Phase 3 skills delegate to Engin. Both need to be defined now so Claude Code recognizes them from install day one.
Output: orbit-pm/.claude/agents/astro.md and orbit-pm/.claude/agents/engin.md
</objective>

<execution_context>
@/Users/equipo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/equipo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Authoritative agent specs from design docs. Copy exactly. -->

From mvp/04-PROMPTS.md — Astro frontmatter (verified against official sub-agents docs):
```yaml
---
name: astro
description: >
  Organization agent for the Orbit PM workspace. Reads notes, assigns themes,
  extracts action items, logs decisions, and rebuilds the knowledge index.
  Delegate to this agent after note ingestion or when reorganization is needed.
model: sonnet
memory: project
tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
disallowedTools:
  - Bash
  - Agent
  - WebSearch
  - WebFetch
---
```

From mvp/04-PROMPTS.md — Engin frontmatter:
```yaml
---
name: engin
description: >
  Expert PM advisor for the Orbit workspace. Senior product management advisor
  who synthesizes knowledge across meetings, tracks decisions, identifies
  contradictions, and guides PM workflows including prioritization, meeting prep,
  and strategic synthesis. Delegate for any PM question or analysis.
model: inherit
memory: project
tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - WebSearch
disallowedTools:
  - Bash
---
```

Full system prompts for both agents are in mvp/04-PROMPTS.md sections 1 and 2.
The executor MUST read mvp/04-PROMPTS.md to get the complete system prompt bodies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Astro agent definition</name>
  <files>orbit-pm/.claude/agents/astro.md</files>
  <read_first>
    - /Users/equipo/Orbit/mvp/04-PROMPTS.md (complete Astro frontmatter AND system prompt body — sections 1)
    - /Users/equipo/Orbit/mvp/02-ARCHITECTURE.md (Astro agent section, tool restrictions rationale)
    - /Users/equipo/Orbit/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Claude Code Agent Format, verified frontmatter fields)
  </read_first>
  <action>
Create /Users/equipo/Orbit/orbit-pm/.claude/agents/astro.md.

The file has two parts:
1. YAML frontmatter (between --- delimiters) with exactly these fields:
   - name: astro
   - description: (multi-line, the text from mvp/04-PROMPTS.md exactly)
   - model: sonnet
   - memory: project
   - tools: [Read, Write, Edit, Glob, Grep]
   - disallowedTools: [Bash, Agent, WebSearch, WebFetch]

2. System prompt body: Copy the COMPLETE Astro system prompt from mvp/04-PROMPTS.md section 1 verbatim. This includes all sections: Context Assembly, What You Do (Theme Assignment, Action Item Extraction, Decision Logging, Index Rebuild, Memory Update), and Rules.

Do NOT truncate or summarize the system prompt. It must be complete so Astro functions correctly when invoked.
  </action>
  <verify>
    <automated>test -f /Users/equipo/Orbit/orbit-pm/.claude/agents/astro.md && grep -q "name: astro" /Users/equipo/Orbit/orbit-pm/.claude/agents/astro.md && grep -q "model: sonnet" /Users/equipo/Orbit/orbit-pm/.claude/agents/astro.md && grep -q "memory: project" /Users/equipo/Orbit/orbit-pm/.claude/agents/astro.md && grep -q "WebFetch" /Users/equipo/Orbit/orbit-pm/.claude/agents/astro.md && grep -q "Theme Assignment" /Users/equipo/Orbit/orbit-pm/.claude/agents/astro.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "name: astro" orbit-pm/.claude/agents/astro.md returns 0
    - grep -q "model: sonnet" orbit-pm/.claude/agents/astro.md returns 0
    - grep -q "memory: project" orbit-pm/.claude/agents/astro.md returns 0
    - grep -q "disallowedTools" orbit-pm/.claude/agents/astro.md returns 0
    - grep -q "WebFetch" orbit-pm/.claude/agents/astro.md returns 0 (confirms disallowedTools list is complete)
    - grep -q "Delegate to this agent after note ingestion" orbit-pm/.claude/agents/astro.md returns 0
    - grep -q "Theme Assignment" orbit-pm/.claude/agents/astro.md returns 0 (system prompt body present)
    - grep -q "NEVER modify note content body" orbit-pm/.claude/agents/astro.md returns 0 (Rules section present)
    - wc -l orbit-pm/.claude/agents/astro.md shows at least 50 lines (full system prompt, not truncated)
  </acceptance_criteria>
  <done>astro.md exists with complete YAML frontmatter (all 6 fields) and the full Astro system prompt body including all 5 subsections and Rules.</done>
</task>

<task type="auto">
  <name>Task 2: Create Engin agent definition</name>
  <files>orbit-pm/.claude/agents/engin.md</files>
  <read_first>
    - /Users/equipo/Orbit/mvp/04-PROMPTS.md (complete Engin frontmatter AND system prompt body — section 2)
    - /Users/equipo/Orbit/mvp/02-ARCHITECTURE.md (Engin agent section, WebSearch rationale)
    - /Users/equipo/Orbit/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Agent Format)
  </read_first>
  <action>
Create /Users/equipo/Orbit/orbit-pm/.claude/agents/engin.md.

The file has two parts:
1. YAML frontmatter with exactly these fields:
   - name: engin
   - description: (multi-line, the text from mvp/04-PROMPTS.md exactly)
   - model: inherit
   - memory: project
   - tools: [Read, Write, Edit, Glob, Grep, WebSearch]
   - disallowedTools: [Bash]

Note: model is "inherit" (not "sonnet") — Engin uses whatever model the user's Claude Code session has configured, enabling Opus-level reasoning.

2. System prompt body: Copy the COMPLETE Engin system prompt from mvp/04-PROMPTS.md section 2 verbatim. This includes: Context Assembly (5 steps), Your 7 Capabilities (Strategic Synthesis, Customer Insight Analysis, RICE Prioritization, Decision Tracking, Meeting Preparation, Morning/Weekly Brief, Contradiction & Risk Detection), Communication Rules, and Memory section.

Do NOT truncate or summarize. The full system prompt is what makes Engin function as a senior PM advisor rather than a generic assistant.
  </action>
  <verify>
    <automated>test -f /Users/equipo/Orbit/orbit-pm/.claude/agents/engin.md && grep -q "name: engin" /Users/equipo/Orbit/orbit-pm/.claude/agents/engin.md && grep -q "model: inherit" /Users/equipo/Orbit/orbit-pm/.claude/agents/engin.md && grep -q "memory: project" /Users/equipo/Orbit/orbit-pm/.claude/agents/engin.md && grep -q "WebSearch" /Users/equipo/Orbit/orbit-pm/.claude/agents/engin.md && grep -q "7 Capabilities" /Users/equipo/Orbit/orbit-pm/.claude/agents/engin.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "name: engin" orbit-pm/.claude/agents/engin.md returns 0
    - grep -q "model: inherit" orbit-pm/.claude/agents/engin.md returns 0
    - grep -q "memory: project" orbit-pm/.claude/agents/engin.md returns 0
    - grep -q "WebSearch" orbit-pm/.claude/agents/engin.md returns 0 (in tools list)
    - grep -q "disallowedTools" orbit-pm/.claude/agents/engin.md returns 0
    - grep -q "Bash" orbit-pm/.claude/agents/engin.md returns 0 (in disallowedTools)
    - grep -q "Delegate for any PM question or analysis" orbit-pm/.claude/agents/engin.md returns 0 (description field)
    - grep -q "7 Capabilities" orbit-pm/.claude/agents/engin.md returns 0 (system prompt body present)
    - grep -q "RICE Prioritization" orbit-pm/.claude/agents/engin.md returns 0
    - grep -q "Always cite sources" orbit-pm/.claude/agents/engin.md returns 0 (Communication Rules present)
    - wc -l orbit-pm/.claude/agents/engin.md shows at least 80 lines (full system prompt)
  </acceptance_criteria>
  <done>engin.md exists with complete YAML frontmatter (model: inherit, all tools/disallowedTools) and the full Engin system prompt body including all 7 capabilities, Communication Rules, and Memory section.</done>
</task>

</tasks>

<verification>
bash -c 'cd /Users/equipo/Orbit && grep -q "model: sonnet" orbit-pm/.claude/agents/astro.md && grep -q "model: inherit" orbit-pm/.claude/agents/engin.md && grep -q "memory: project" orbit-pm/.claude/agents/astro.md && grep -q "memory: project" orbit-pm/.claude/agents/engin.md && grep -q "NEVER modify note content body" orbit-pm/.claude/agents/astro.md && grep -q "Always cite sources" orbit-pm/.claude/agents/engin.md && echo "Plan 02 PASS"'
</verification>

<success_criteria>
- Both agent files exist with correct YAML frontmatter (verified field-by-field)
- Astro has model: sonnet, disallowedTools includes Bash/Agent/WebSearch/WebFetch
- Engin has model: inherit, tools includes WebSearch, disallowedTools includes only Bash
- Both have memory: project
- Both system prompt bodies are complete (not truncated summaries)
</success_criteria>

<output>
After completion, create /Users/equipo/Orbit/.planning/phases/01-foundation/01-02-SUMMARY.md
</output>
