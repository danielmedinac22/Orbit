---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orbit-pm/CLAUDE.md
  - orbit-pm/LICENSE
  - orbit-pm/.claude/settings.json
  - orbit-pm/README.md
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - "orbit-pm/CLAUDE.md exists at repo root and contains the Orbit section marker so Claude Code loads it on session start"
    - "orbit-pm/LICENSE contains MIT license text"
    - "orbit-pm/.claude/settings.json exists with the PostToolUse hook structure for Write events"
    - "orbit-pm/README.md exists as a placeholder (Phase 4 will fill it)"
  artifacts:
    - path: "orbit-pm/CLAUDE.md"
      provides: "Project-level instructions for Claude Code"
      contains: "# Orbit — Context Copilot for Product Managers"
    - path: "orbit-pm/LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "orbit-pm/.claude/settings.json"
      provides: "PostToolUse hook structure"
      contains: "PostToolUse"
    - path: "orbit-pm/README.md"
      provides: "Placeholder README"
  key_links:
    - from: "orbit-pm/CLAUDE.md"
      to: "Claude Code session"
      via: "auto-loaded at session start from repo root"
      pattern: "Orbit — Context Copilot"
    - from: "orbit-pm/.claude/settings.json"
      to: "PostToolUse Write events"
      via: "hooks.PostToolUse matcher: Write"
      pattern: "PostToolUse"
---

<objective>
Create the orbit-pm/ repository scaffold: the directory structure, CLAUDE.md with Orbit project instructions, MIT LICENSE, a placeholder README.md, and .claude/settings.json with the PostToolUse hook structure.

Purpose: Establishes the repo root and the two files that Claude Code loads automatically (CLAUDE.md) and the hook contract that Phase 2 will complete (settings.json).
Output: orbit-pm/ directory with CLAUDE.md, LICENSE, README.md, .claude/settings.json
</objective>

<execution_context>
@/Users/equipo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/equipo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
<!-- Authoritative specs from design docs. Executor must replicate exactly. -->

From mvp/02-ARCHITECTURE.md — CLAUDE.md content to append (exact text):
```markdown
# Orbit — Context Copilot for Product Managers

## .orbit/ Directory
- `notes/` — Chronological meeting notes (the core data)
- `themes/` — Thematic organization (themes reference notes)
- `action-items/pending.md` — All pending action items
- `decisions/log.md` — Cross-meeting decision tracker
- `briefs/` — Daily and weekly briefs
- `artifacts/` — Generated PM documents
- `index.md` — Knowledge index (auto-rebuilt by Astro)
- `config.md` — PM's identity, role, goals, stakeholders

## Rules
- Notes are the source of truth. Never modify note content body.
- Only Astro updates the `themes:` field in note frontmatter.
- All factual claims cite the source: "(from Meeting Title, YYYY-MM-DD)"
- Respond in the same language as the user's query.
- Never invent information not present in the notes.
- When a user asks a PM question, delegate to the Engin agent.
- When notes need organization, delegate to the Astro agent.

## Skills: /orbit-init, /orbit-ingest, /orbit-status, /orbit-brief,
## /orbit-priorities, /orbit-decisions, /orbit-prep, /orbit-artifact
```

From mvp/01-RESEARCH.md — settings.json Phase 1 hook (minimal, establishes structure):
```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); exit(0 if '.orbit/notes/' not in p else 0)\" && echo 'Orbit: Note written. Consider running Astro to organize.' || true"
          }
        ]
      }
    ]
  }
}
```

From mvp/06-BRANDING.md — README opening (exact text for placeholder):
```markdown
# Orbit

An open-source Claude Code plugin that makes Product Managers 100x productive.

Ingest meeting notes. Organize by themes. Track decisions. Prioritize with data.
Generate PRDs. Get daily briefings. All from your terminal.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create orbit-pm/ directory structure and root files</name>
  <files>
    orbit-pm/CLAUDE.md
    orbit-pm/LICENSE
    orbit-pm/README.md
    orbit-pm/.claude/settings.json
  </files>
  <read_first>
    - /Users/equipo/Orbit/mvp/02-ARCHITECTURE.md (CLAUDE.md content spec, repo structure)
    - /Users/equipo/Orbit/mvp/06-BRANDING.md (README opening, orbit-pm/ structure)
    - /Users/equipo/Orbit/.planning/phases/01-foundation/01-RESEARCH.md (settings.json hook pattern, CLAUDE.md idempotency marker)
  </read_first>
  <action>
Create the orbit-pm/ directory at /Users/equipo/Orbit/orbit-pm/ with these subdirectories: .claude/, .claude/agents/, .claude/skills/, templates/

Create orbit-pm/CLAUDE.md with exactly this content (the marker line is critical for install.sh idempotency):

```markdown
# Orbit — Context Copilot for Product Managers

## .orbit/ Directory
- `notes/` — Chronological meeting notes (the core data)
- `themes/` — Thematic organization (themes reference notes)
- `action-items/pending.md` — All pending action items
- `decisions/log.md` — Cross-meeting decision tracker
- `briefs/` — Daily and weekly briefs
- `artifacts/` — Generated PM documents
- `index.md` — Knowledge index (auto-rebuilt by Astro)
- `config.md` — PM's identity, role, goals, stakeholders

## Rules
- Notes are the source of truth. Never modify note content body.
- Only Astro updates the `themes:` field in note frontmatter.
- All factual claims cite the source: "(from Meeting Title, YYYY-MM-DD)"
- Respond in the same language as the user's query.
- Never invent information not present in the notes.
- When a user asks a PM question, delegate to the Engin agent.
- When notes need organization, delegate to the Astro agent.

## Skills: /orbit-init, /orbit-ingest, /orbit-status, /orbit-brief,
## /orbit-priorities, /orbit-decisions, /orbit-prep, /orbit-artifact
```

Create orbit-pm/LICENSE with standard MIT license text dated 2026, copyright "Orbit Contributors".

Create orbit-pm/README.md as a placeholder with the branding-compliant opening and a "Full documentation coming in v1.0" note. Use the exact opening from mvp/06-BRANDING.md:
```
# Orbit

An open-source Claude Code plugin that makes Product Managers 100x productive.

Ingest meeting notes. Organize by themes. Track decisions. Prioritize with data.
Generate PRDs. Get daily briefings. All from your terminal.

---

*Full documentation coming in Phase 4. See install.sh for installation.*
```

Create orbit-pm/.claude/settings.json with the minimal PostToolUse hook structure exactly as specified in the RESEARCH.md Pattern 3. This is the Phase 1 structural definition — Phase 2 (ASTR-08) will refine the hook behavior:

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys, json; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); exit(0 if '.orbit/notes/' not in p else 0)\" && echo 'Orbit: Note written. Consider running Astro to organize.' || true"
          }
        ]
      }
    ]
  }
}
```
  </action>
  <verify>
    <automated>test -f /Users/equipo/Orbit/orbit-pm/CLAUDE.md && grep -q "Orbit — Context Copilot for Product Managers" /Users/equipo/Orbit/orbit-pm/CLAUDE.md && test -f /Users/equipo/Orbit/orbit-pm/LICENSE && grep -q "MIT License" /Users/equipo/Orbit/orbit-pm/LICENSE && test -f /Users/equipo/Orbit/orbit-pm/.claude/settings.json && grep -q "PostToolUse" /Users/equipo/Orbit/orbit-pm/.claude/settings.json && test -f /Users/equipo/Orbit/orbit-pm/README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "# Orbit — Context Copilot for Product Managers" orbit-pm/CLAUDE.md returns 0
    - grep -q "Only Astro updates" orbit-pm/CLAUDE.md returns 0
    - grep -q "delegate to the Engin agent" orbit-pm/CLAUDE.md returns 0
    - grep -q "MIT License" orbit-pm/LICENSE returns 0
    - grep -q "PostToolUse" orbit-pm/.claude/settings.json returns 0
    - grep -q "matcher" orbit-pm/.claude/settings.json returns 0
    - python3 -c "import json; json.load(open('orbit-pm/.claude/settings.json'))" exits 0 (valid JSON)
    - test -f orbit-pm/README.md returns 0
    - test -d orbit-pm/.claude/agents returns 0
    - test -d orbit-pm/.claude/skills returns 0
    - test -d orbit-pm/templates returns 0
  </acceptance_criteria>
  <done>orbit-pm/ directory exists with CLAUDE.md containing the exact Orbit section (idempotency marker present), MIT LICENSE, placeholder README, and valid settings.json with PostToolUse hook structure. All .claude/ subdirectories exist.</done>
</task>

</tasks>

<verification>
bash -c 'cd /Users/equipo/Orbit && test -f orbit-pm/CLAUDE.md && grep -q "Orbit — Context Copilot for Product Managers" orbit-pm/CLAUDE.md && grep -q "PostToolUse" orbit-pm/.claude/settings.json && python3 -c "import json; json.load(open(\"orbit-pm/.claude/settings.json\"))" && echo "Plan 01 PASS"'
</verification>

<success_criteria>
- orbit-pm/CLAUDE.md exists with the exact marker "# Orbit — Context Copilot for Product Managers" that install.sh uses for idempotency
- orbit-pm/LICENSE exists with MIT text
- orbit-pm/README.md exists (placeholder)
- orbit-pm/.claude/settings.json is valid JSON with PostToolUse hook structure
- orbit-pm/.claude/agents/, orbit-pm/.claude/skills/, orbit-pm/templates/ directories exist
</success_criteria>

<output>
After completion, create /Users/equipo/Orbit/.planning/phases/01-foundation/01-01-SUMMARY.md
</output>
