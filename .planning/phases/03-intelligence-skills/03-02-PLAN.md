---
phase: 03-intelligence-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - orbit-pm/.claude/skills/orbit-priorities/SKILL.md
  - orbit-pm/templates/rice-scorecard.md
autonomous: true
requirements:
  - PRIO-01
  - PRIO-02
  - PRIO-03
  - PRIO-04

must_haves:
  truths:
    - "orbit-priorities SKILL.md describes how to handle PM score overrides with asterisk notation"
    - "orbit-priorities SKILL.md scores each RICE dimension with meeting evidence citations"
    - "orbit-priorities SKILL.md writes scorecard to .orbit/artifacts/rice-scorecard-YYYY-MM-DD.md"
    - "rice-scorecard template includes asterisk notation example and gut-call footnote pattern"
    - "orbit-priorities SKILL.md Challenge section uses direct pushback in Engin voice"
  artifacts:
    - path: "orbit-pm/.claude/skills/orbit-priorities/SKILL.md"
      provides: "RICE prioritization skill with evidence-based scoring and override handling"
      contains: "gut call"
    - path: "orbit-pm/templates/rice-scorecard.md"
      provides: "RICE scorecard template with asterisk override notation"
      contains: "PM override"
  key_links:
    - from: "orbit-priorities SKILL.md override handling"
      to: "rice-scorecard.md asterisk notation"
      via: "Both files describe the same override pattern — skill instructs behavior, template shows format"
      pattern: "gut call|PM override|\\*"
    - from: "orbit-priorities SKILL.md RICE Analysis Flow"
      to: ".orbit/notes/ and .orbit/themes/"
      via: "Context Assembly reads notes for evidence citations"
      pattern: "evidence|cite|meeting"
---

<objective>
Verify and patch orbit-priorities SKILL.md and rice-scorecard template against PRIO requirements plus locked decisions D-04 and D-05.

Purpose: D-05 (gut-call override with asterisk notation) is missing from both the skill file and the template. Both files must be patched together to maintain consistency — the skill describes the behavior, the template shows the format.
Output: orbit-priorities SKILL.md with override handling and rice-scorecard template with asterisk notation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligence-skills/03-CONTEXT.md
@.planning/phases/03-intelligence-skills/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and patch orbit-priorities SKILL.md against PRIO-01/02/03/04 + D-04/D-05</name>
  <files>orbit-pm/.claude/skills/orbit-priorities/SKILL.md</files>
  <read_first>
    - orbit-pm/.claude/skills/orbit-priorities/SKILL.md (current state)
    - .planning/phases/03-intelligence-skills/03-CONTEXT.md (locked decisions D-04, D-05)
    - .planning/phases/03-intelligence-skills/03-RESEARCH.md (gap analysis for orbit-priorities)
  </read_first>
  <action>
Systematic verification checklist — read the file, check each item, patch only where gaps exist:

**PRIO-01** (identifies candidate features/initiatives from notes and themes):
- Verify RICE Analysis Flow step 1 identifies candidates from action items, recurring topics, open decisions, themes — present (lines 27-28). **No gap.**

**PRIO-02** (scores each candidate using RICE with evidence from meetings):
- Verify RICE dimensions are defined with scoring guidance — present (lines 30-34).
- Verify "Cite specific evidence for each dimension: meeting name and date" — present (line 36). **No gap.**

**PRIO-03** (generates ranked scorecard at `.orbit/artifacts/rice-scorecard-YYYY-MM-DD.md`):
- Verify Write Scorecard section specifies path — present (line 78). **No gap.**

**PRIO-04** (challenges PM when data contradicts assumptions):
- Verify Challenge section exists with direct pushback examples — present (lines 62-68). **No gap.**
- Verify D-04 example tone is present — the example "You've discussed X in N meetings but pushed its timeline twice" is present (line 66). **No gap.**

**D-05** (PM can override RICE scores, labeled as "gut call" vs "evidence-based", asterisk notation):
- **GAP**: No mention of PM overrides, asterisk notation, or "gut call" anywhere in the file.

  Add a new section `## PM Score Overrides` after the `## Output Format` section (after line 68, before `### Scoring rules:`). Insert:

  ```markdown
  ## PM Score Overrides

  If the PM provides their own scores or overrides Engin's evidence-based scores:

  1. Apply the PM's override to the scorecard
  2. Mark the initiative with `*` (asterisk) in the scorecard table
  3. Add a footnote at the bottom of the Scorecard section: `* PM override: [dimension] adjusted from [evidence score] to [PM score]. Marked as gut call.`
  4. In the Challenge section, acknowledge the override: "You overrode the [dimension] score for [initiative] — noted as gut call. Watch for signals that confirm or contradict."

  Override labels:
  - **Evidence-based**: score derived from meeting evidence (default — no marker)
  - **Gut call**: PM override without supporting meeting data (marked with `*`)
  ```

  Also update the Output Format scorecard table example to show one override row. After the existing row `| 2 |` add:
  ```
  | 3 | [name]* | 5 | 7 | 5 | 8 | 22 | PM override — gut call |
  ```
  And add a footnote line after the table:
  ```
  *\* PM override: Impact adjusted from 4 to 7. Gut call.*
  ```

Only apply edits for the D-05 override gap. Do not touch sections that already pass verification.
  </action>
  <verify>
    <automated>grep -q "gut call" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && grep -q "PM override" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && grep -q "asterisk" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && grep -q "rice-scorecard-" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && grep -q "Cite specific evidence" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - orbit-priorities SKILL.md contains "gut call" in a PM Score Overrides section or equivalent
    - orbit-priorities SKILL.md contains "PM override" describing the override behavior
    - orbit-priorities SKILL.md contains "asterisk" or "`*`" describing the notation format
    - orbit-priorities SKILL.md Output Format scorecard table includes an example row with `*` marker
    - orbit-priorities SKILL.md still contains "rice-scorecard-" for the output path (PRIO-03 unchanged)
    - orbit-priorities SKILL.md still contains "Cite specific evidence" (PRIO-02 unchanged)
    - orbit-priorities SKILL.md Challenge section still has direct pushback examples (PRIO-04/D-04 unchanged)
  </acceptance_criteria>
  <done>orbit-priorities SKILL.md passes all PRIO-01/02/03/04 requirements and honors D-04/D-05 locked decisions. PM score overrides are now documented with asterisk notation and gut-call labeling.</done>
</task>

<task type="auto">
  <name>Task 2: Patch rice-scorecard template with asterisk override notation per D-05</name>
  <files>orbit-pm/templates/rice-scorecard.md</files>
  <read_first>
    - orbit-pm/templates/rice-scorecard.md (current state)
    - .planning/phases/03-intelligence-skills/03-CONTEXT.md (locked decision D-05)
    - .planning/phases/03-intelligence-skills/03-RESEARCH.md (gap analysis and code example for rice-scorecard)
  </read_first>
  <action>
The rice-scorecard template needs asterisk notation for PM overrides to match D-05 and stay consistent with the orbit-priorities SKILL.md changes in Task 1.

**Patch 1**: In the Scorecard section table (after line 33), add an example override row:

Change the empty example rows to include one override example. Replace the current blank rows:
```
| 1 | | | | | | | |
| 2 | | | | | | | |
| 3 | | | | | | | |
```

With:
```
| 1 | | | | | | | |
| 2 | | | | | | | |
| 3 | | | | | | | |

<!-- If a PM overrides a score, mark with * and add footnote below -->
<!-- Example: | 2 | Feature B* | 7 | 10 | 5 | 2 | 175 | PM override — gut call | -->
<!-- Footnote: * PM override: [dimension] adjusted from [evidence score] to [PM score]. Gut call. -->
```

**Patch 2**: Add a new section after the Evidence Cited section (before the closing `---` line), for override documentation:

```markdown
## Overrides

<!-- Fill with: Any PM score overrides applied during this analysis. Each override notes the dimension, original evidence-based score, PM's override score, and whether it's labeled "gut call" or "evidence-based". If no overrides, omit this section. -->

| Initiative | Dimension | Evidence Score | PM Score | Label |
|-----------|-----------|----------------|----------|-------|
| | | | | |
```

These are HTML comment instructions following the established pattern from Phase 1 (01-03 decision: templates use `<!-- Fill with: -->` to instruct Engin).
  </action>
  <verify>
    <automated>grep -q "PM override" orbit-pm/templates/rice-scorecard.md && grep -q "gut call" orbit-pm/templates/rice-scorecard.md && grep -q "Overrides" orbit-pm/templates/rice-scorecard.md && grep -q "Evidence Cited" orbit-pm/templates/rice-scorecard.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - rice-scorecard.md contains "PM override" in an HTML comment or section
    - rice-scorecard.md contains "gut call" in an HTML comment or section
    - rice-scorecard.md contains a new "## Overrides" section with a table for documenting overrides
    - rice-scorecard.md Overrides table has columns: Initiative, Dimension, Evidence Score, PM Score, Label
    - rice-scorecard.md still contains "## Scoring Guide", "## Scorecard", "## Analysis", "## Challenge", "## Evidence Cited" (existing sections unchanged)
    - rice-scorecard.md HTML comments follow the `<!-- Fill with: -->` pattern from Phase 1
  </acceptance_criteria>
  <done>rice-scorecard template includes asterisk override notation and Overrides section per D-05. Template is consistent with orbit-priorities SKILL.md override handling.</done>
</task>

</tasks>

<verification>
Run all PRIO requirement checks:
```bash
# PRIO-01: Identifies candidates from notes and themes
grep -q "Identify candidate" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && echo "PRIO-01 PASS" || echo "PRIO-01 FAIL"

# PRIO-02: RICE dimensions with evidence citations
grep -q "Cite specific evidence" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && echo "PRIO-02 PASS" || echo "PRIO-02 FAIL"

# PRIO-03: Scorecard path
grep -q "rice-scorecard-" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && echo "PRIO-03 PASS" || echo "PRIO-03 FAIL"

# PRIO-04: Challenge + D-05 gut-call override
grep -q "gut call" orbit-pm/.claude/skills/orbit-priorities/SKILL.md && echo "PRIO-04a PASS" || echo "PRIO-04a FAIL"
grep -q "gut call" orbit-pm/templates/rice-scorecard.md && echo "PRIO-04b PASS" || echo "PRIO-04b FAIL"
```
</verification>

<success_criteria>
All 5 PRIO checks return PASS. Both orbit-priorities SKILL.md and rice-scorecard.md template are consistent on override notation. D-04 (direct pushback) and D-05 (asterisk/gut-call notation) are both honored.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence-skills/03-02-SUMMARY.md`
</output>
