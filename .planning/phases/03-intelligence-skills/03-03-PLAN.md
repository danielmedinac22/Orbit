---
phase: 03-intelligence-skills
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - orbit-pm/.claude/skills/orbit-prep/SKILL.md
  - orbit-pm/.claude/skills/orbit-artifact/SKILL.md
  - orbit-pm/templates/prd.md
  - orbit-pm/templates/decision-record.md
  - orbit-pm/templates/weekly-summary.md
  - orbit-pm/templates/stakeholder-update.md
autonomous: true
requirements:
  - PREP-01
  - PREP-02
  - PREP-03
  - PREP-04
  - ARTF-01
  - ARTF-02
  - ARTF-03
  - ARTF-04

must_haves:
  truths:
    - "orbit-prep SKILL.md gathers topic context from notes and themes"
    - "orbit-prep SKILL.md pulls attendee-specific action items, mentions, questions, and stance"
    - "orbit-prep SKILL.md generates agenda, talking points, risk areas, and decisions needed"
    - "orbit-prep SKILL.md optionally writes to .orbit/briefs/prep-YYYY-MM-DD-topic.md"
    - "orbit-artifact SKILL.md reads templates from .orbit/templates/"
    - "orbit-artifact SKILL.md gathers context per theme or across all themes"
    - "orbit-artifact SKILL.md handles versioning with -v2, -v3 auto-increment"
    - "All 5 templates exist with correct frontmatter and HTML comment instructions"
  artifacts:
    - path: "orbit-pm/.claude/skills/orbit-prep/SKILL.md"
      provides: "Meeting preparation skill with topic and attendee research"
      contains: "Attendee Research"
    - path: "orbit-pm/.claude/skills/orbit-artifact/SKILL.md"
      provides: "Artifact generation skill with template validation and versioning"
      contains: "Versioning Check"
    - path: "orbit-pm/templates/prd.md"
      provides: "PRD template"
      contains: "template: prd"
    - path: "orbit-pm/templates/decision-record.md"
      provides: "Decision record template"
      contains: "template: decision-record"
    - path: "orbit-pm/templates/weekly-summary.md"
      provides: "Weekly summary template"
      contains: "template: weekly-summary"
    - path: "orbit-pm/templates/stakeholder-update.md"
      provides: "Stakeholder update template"
      contains: "template: stakeholder-update"
  key_links:
    - from: "orbit-artifact SKILL.md"
      to: ".orbit/templates/"
      via: "Context Assembly reads template file"
      pattern: "\\.orbit/templates/"
    - from: "orbit-prep SKILL.md"
      to: ".orbit/briefs/"
      via: "Save Prep writes to briefs directory"
      pattern: "prep-.*\\.md"
---

<objective>
Verify orbit-prep and orbit-artifact skill files plus 4 remaining templates against PREP and ARTF requirements.

Purpose: Research found no gaps in these files — this is a clean verification pass. The work is to systematically confirm each of the 8 requirements is met, and apply patches only if unexpected gaps are found during verification.
Output: Verified skill files and templates confirmed against all PREP and ARTF requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligence-skills/03-CONTEXT.md
@.planning/phases/03-intelligence-skills/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify orbit-prep SKILL.md against PREP-01/02/03/04</name>
  <files>orbit-pm/.claude/skills/orbit-prep/SKILL.md</files>
  <read_first>
    - orbit-pm/.claude/skills/orbit-prep/SKILL.md (current state — full read, line-by-line verification)
    - .planning/phases/03-intelligence-skills/03-CONTEXT.md (Claude's Discretion items for prep depth)
    - .planning/REQUIREMENTS.md (PREP-01 through PREP-04 exact text)
  </read_first>
  <action>
Systematic verification — read the entire file, check each requirement. Expected: no gaps found.

**PREP-01** (gathers context relevant to meeting topic from notes and themes):
- Verify Topic Research section exists with theme matching and note reading — expected present at lines 30-38.
- Verify cross-theme context gathering (decisions, questions, overdue items) — expected present at lines 36-38.
- Confirm or flag.

**PREP-02** (for named attendees: action items, recent mentions, open questions):
- Verify Attendee Research section exists — expected present at lines 42-51.
- Verify it searches `participants:` fields — expected present at line 43.
- Verify it gathers: action items (status/overdue), what they said (last 14 days), open questions, stance on decisions — expected present at lines 46-49.
- Confirm or flag.

**PREP-03** (generates agenda, talking points, risk areas, decisions needed):
- Verify Output Format includes: Suggested Agenda, Talking Points, Coordinates to Lock — expected present at lines 69-81.
- Note: "risk areas" from PREP-03 maps to the Talking Points section bullet "Flag the risk that needs acknowledgment" (line 76) plus any risk items that surface naturally in the agenda. This is sufficient coverage.
- Confirm or flag.

**PREP-04** (optionally writes to `.orbit/briefs/prep-YYYY-MM-DD-topic.md`):
- Verify Save Prep section exists with user confirmation prompt — expected present at lines 94-100.
- Verify path matches requirement format — expected present at line 98.
- Confirm or flag.

**If all 4 pass**: No edits needed. Report file as verified.
**If gap found**: Apply minimal Edit patch and document what was added and why.
  </action>
  <verify>
    <automated>grep -q "Topic Research" orbit-pm/.claude/skills/orbit-prep/SKILL.md && grep -q "Attendee Research" orbit-pm/.claude/skills/orbit-prep/SKILL.md && grep -q "Talking Points" orbit-pm/.claude/skills/orbit-prep/SKILL.md && grep -q "prep-" orbit-pm/.claude/skills/orbit-prep/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - orbit-prep SKILL.md contains "Topic Research" section heading
    - orbit-prep SKILL.md contains "Attendee Research" section heading
    - orbit-prep SKILL.md Attendee Research section mentions "action items" and "open questions"
    - orbit-prep SKILL.md contains "Talking Points" in Output Format
    - orbit-prep SKILL.md contains "Suggested Agenda" in Output Format
    - orbit-prep SKILL.md contains "Coordinates to Lock" in Output Format (decisions needed)
    - orbit-prep SKILL.md contains "prep-" in Save Prep section path
    - orbit-prep SKILL.md contains "participants:" field search for attendee matching
  </acceptance_criteria>
  <done>orbit-prep SKILL.md verified against all PREP-01/02/03/04 requirements. All sections present and complete.</done>
</task>

<task type="auto">
  <name>Task 2: Verify orbit-artifact SKILL.md + 4 remaining templates against ARTF-01/02/03/04</name>
  <files>
    orbit-pm/.claude/skills/orbit-artifact/SKILL.md
    orbit-pm/templates/prd.md
    orbit-pm/templates/decision-record.md
    orbit-pm/templates/weekly-summary.md
    orbit-pm/templates/stakeholder-update.md
  </files>
  <read_first>
    - orbit-pm/.claude/skills/orbit-artifact/SKILL.md (current state)
    - orbit-pm/templates/prd.md (verify exists with correct frontmatter)
    - orbit-pm/templates/decision-record.md (verify exists with correct frontmatter)
    - orbit-pm/templates/weekly-summary.md (verify exists with correct frontmatter)
    - orbit-pm/templates/stakeholder-update.md (verify exists with correct frontmatter)
    - .planning/REQUIREMENTS.md (ARTF-01 through ARTF-04 exact text)
  </read_first>
  <action>
Systematic verification — read all files, check each requirement. Expected: no gaps found.

**ARTF-01** (reads template from `.orbit/templates/` and generates document):
- Verify Context Assembly reads template from `.orbit/templates/<name>.md` — expected present in orbit-artifact SKILL.md line 34.
- Verify Template Validation lists all 5 valid template names — expected present at line 27.
- Confirm or flag.

**ARTF-02** (gathers context from notes, action items, decisions for specified theme or all themes):
- Verify Context Assembly has two paths: `--theme` specified and no theme — expected present at lines 41-52.
- Verify theme path reads theme notes, action items, decisions — expected present.
- Verify no-theme path reads across all notes, pending items, full log — expected present.
- Confirm or flag.

**ARTF-03** (handles versioning: -v2, -v3):
- Verify Versioning Check section exists — expected present at lines 55-63.
- Verify it checks for existing file and increments version number — expected present.
- Verify it checks for `-v2.md`, `-v3.md`, etc. — expected present at line 61.
- Confirm or flag.

**ARTF-04** (5 built-in templates: PRD, Decision Record, Weekly Summary, Stakeholder Update, RICE Scorecard):
- Verify each of the 4 remaining template files exists (rice-scorecard verified in Plan 02):
  - `orbit-pm/templates/prd.md` — read, verify `template: prd` in frontmatter
  - `orbit-pm/templates/decision-record.md` — read, verify `template: decision-record` in frontmatter
  - `orbit-pm/templates/weekly-summary.md` — read, verify `template: weekly-summary` in frontmatter
  - `orbit-pm/templates/stakeholder-update.md` — read, verify `template: stakeholder-update` in frontmatter
- Verify each template has:
  - YAML frontmatter with `template:` and `version:` fields
  - HTML comment instructions (`<!-- Fill with: -->`) per Phase 1 pattern
  - `{variable}` placeholder substitution patterns
- Confirm or flag.

**If all pass**: No edits needed. Report all files as verified.
**If gap found**: Apply minimal Edit patch and document what was added and why.
  </action>
  <verify>
    <automated>grep -q "\.orbit/templates/" orbit-pm/.claude/skills/orbit-artifact/SKILL.md && grep -q "\-v2" orbit-pm/.claude/skills/orbit-artifact/SKILL.md && grep -q "template: prd" orbit-pm/templates/prd.md && grep -q "template: decision-record" orbit-pm/templates/decision-record.md && grep -q "template: weekly-summary" orbit-pm/templates/weekly-summary.md && grep -q "template: stakeholder-update" orbit-pm/templates/stakeholder-update.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - orbit-artifact SKILL.md contains ".orbit/templates/" in Context Assembly section
    - orbit-artifact SKILL.md contains "-v2" in Versioning Check section
    - orbit-artifact SKILL.md Template Validation lists: prd, decision-record, weekly-summary, stakeholder-update, rice-scorecard
    - prd.md contains "template: prd" in YAML frontmatter
    - decision-record.md contains "template: decision-record" in YAML frontmatter
    - weekly-summary.md contains "template: weekly-summary" in YAML frontmatter
    - stakeholder-update.md contains "template: stakeholder-update" in YAML frontmatter
    - All 4 template files contain at least one "<!-- Fill with:" HTML comment instruction
    - All 4 template files contain at least one "{" placeholder variable
  </acceptance_criteria>
  <done>orbit-artifact SKILL.md and all 4 remaining templates verified against ARTF-01/02/03/04 requirements. All files present and complete.</done>
</task>

</tasks>

<verification>
Run all PREP and ARTF requirement checks:
```bash
# PREP-01: Topic context gathering
grep -q "Topic Research" orbit-pm/.claude/skills/orbit-prep/SKILL.md && echo "PREP-01 PASS" || echo "PREP-01 FAIL"

# PREP-02: Attendee-specific context
grep -q "Attendee Research" orbit-pm/.claude/skills/orbit-prep/SKILL.md && echo "PREP-02 PASS" || echo "PREP-02 FAIL"

# PREP-03: Agenda, talking points, risk areas, decisions
grep -q "Talking Points" orbit-pm/.claude/skills/orbit-prep/SKILL.md && echo "PREP-03 PASS" || echo "PREP-03 FAIL"

# PREP-04: Optional write to briefs
grep -q "prep-" orbit-pm/.claude/skills/orbit-prep/SKILL.md && echo "PREP-04 PASS" || echo "PREP-04 FAIL"

# ARTF-01: Reads template
grep -q "\.orbit/templates/" orbit-pm/.claude/skills/orbit-artifact/SKILL.md && echo "ARTF-01 PASS" || echo "ARTF-01 FAIL"

# ARTF-02: Context per theme or all
grep -q "theme specified" orbit-pm/.claude/skills/orbit-artifact/SKILL.md && echo "ARTF-02 PASS" || echo "ARTF-02 FAIL"

# ARTF-03: Versioning
grep -q "\-v2" orbit-pm/.claude/skills/orbit-artifact/SKILL.md && echo "ARTF-03 PASS" || echo "ARTF-03 FAIL"

# ARTF-04: 5 templates exist
ls orbit-pm/templates/ | wc -l | grep -q "5" && echo "ARTF-04 PASS" || echo "ARTF-04 FAIL"
```
</verification>

<success_criteria>
All 8 PREP and ARTF checks return PASS. No unexpected gaps found in orbit-prep, orbit-artifact, or the 4 remaining templates.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence-skills/03-03-SUMMARY.md`
</output>
