---
phase: 03-intelligence-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - orbit-pm/.claude/skills/orbit-brief/SKILL.md
  - orbit-pm/.claude/skills/orbit-decisions/SKILL.md
autonomous: true
requirements:
  - BREF-01
  - BREF-02
  - BREF-03
  - DCSN-01
  - DCSN-02
  - DCSN-03
  - DCSN-04

must_haves:
  truths:
    - "orbit-brief SKILL.md Drifting section includes both overdue action items AND stalled decisions"
    - "orbit-brief SKILL.md weekly format has Decision Velocity, Action Burndown, Cross-Theme Patterns as distinct sections"
    - "orbit-brief SKILL.md instructs both file write and full display in conversation"
    - "orbit-decisions SKILL.md has distinct Reversal Detected section separate from Coordinate Conflicts"
    - "orbit-decisions SKILL.md specifies 7-day stall threshold explicitly"
    - "orbit-decisions SKILL.md resolution suggestion uses 'lock coordinates' language"
  artifacts:
    - path: "orbit-pm/.claude/skills/orbit-brief/SKILL.md"
      provides: "Daily/weekly briefing skill with overdue-first ordering"
      contains: "stalled decisions"
    - path: "orbit-pm/.claude/skills/orbit-decisions/SKILL.md"
      provides: "Decision audit skill with contradiction, reversal, and stall detection"
      contains: "Reversal Detected"
  key_links:
    - from: "orbit-brief SKILL.md Drifting section"
      to: ".orbit/decisions/log.md"
      via: "Context Assembly reads decisions/log.md, Drifting section surfaces stalled ones"
      pattern: "stalled.*decision|stalled.*coordinate"
    - from: "orbit-decisions SKILL.md output format"
      to: "D-08 locked decision"
      via: "Distinct reversal callout section"
      pattern: "↺ Reversal Detected"
---

<objective>
Verify and patch orbit-brief and orbit-decisions skill files against BREF and DCSN requirements plus locked decisions D-01 through D-08.

Purpose: These two skills have identified gaps from research — orbit-brief needs stalled decisions in its Drifting section example (D-01), and orbit-decisions needs a distinct Reversal Detected section separate from Coordinate Conflicts (D-08). All other requirements are already met.
Output: Two verified and patched SKILL.md files ready for PM use.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intelligence-skills/03-CONTEXT.md
@.planning/phases/03-intelligence-skills/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and patch orbit-brief SKILL.md against BREF-01/02/03 + D-01/D-02/D-03</name>
  <files>orbit-pm/.claude/skills/orbit-brief/SKILL.md</files>
  <read_first>
    - orbit-pm/.claude/skills/orbit-brief/SKILL.md (current state — verify each requirement against actual content)
    - .planning/phases/03-intelligence-skills/03-CONTEXT.md (locked decisions D-01, D-02, D-03)
    - .planning/phases/03-intelligence-skills/03-RESEARCH.md (gap analysis for orbit-brief)
  </read_first>
  <action>
Systematic verification checklist — read the file, check each item, patch only where gaps exist:

**BREF-01** (daily brief: overdue first, key updates by theme, pending actions, open questions):
- Verify "⚠️ Drifting" is the FIRST section in the Daily Brief Output Format — it is (line 50).
- Verify Section rules mention "overdue action items" — present (line 76).
- **GAP per D-01**: The Drifting section output format example shows only action items. D-01 requires "drifting missions + stalled coordinates" — both overdue action items AND stalled decisions (confirmed but no follow-up within 7 days). Add a stalled decision example entry to the Drifting output format:
  ```
  - ⏸ **[Decision topic]**: confirmed [date] — no action items after [N] days ([meeting])
  ```
  Add this line after the existing `- Coordinate needed:` line in the Daily Brief Output Format example (after line 52).
- Also add to Section rules (line 76), after "overdue action items (most overdue first)": change to read:
  ```
  **"⚠️ Drifting"**: overdue action items (most overdue first) + stalled decisions (confirmed but no follow-up action items within 7 days), ordered by severity
  ```
  (The current text already says "+ stalled decisions needing a call" — refine to be more precise with the 7-day rule.)

**BREF-02** (weekly brief: decision velocity, action burndown, cross-theme patterns):
- Verify "Decision Velocity" section exists in Weekly Brief Output Format — present (line 88).
- Verify "Action Item Burndown" section exists — present (line 99).
- Verify "Cross-Theme Patterns" section exists — present (line 104).
- Verify weekly is structurally different from daily per D-03 — it is: different sections, different focus. **No gap.**

**BREF-03** (written to file AND displayed in conversation):
- Verify Daily Brief Flow step 6 writes to `.orbit/briefs/YYYY-MM-DD.md` — present (line 32).
- Verify Daily Brief Flow step 7 says "Display in conversation" — present (line 33).
- Verify After Generating section says "Display the full brief in conversation" — present (line 118).
- **No gap.**

**D-02** (skip Drifting when empty):
- Verify "If a section has no entries, omit that section" — present (line 80). **No gap.**

**D-03** (weekly structurally different):
- Already verified above. **No gap.**

If the file is already correct on a check, do NOT modify that section. Only apply the Edit tool for the D-01 stalled-decisions-in-Drifting gap.
  </action>
  <verify>
    <automated>grep -q "stalled decision" orbit-pm/.claude/skills/orbit-brief/SKILL.md && grep -q "confirmed.*no follow-up" orbit-pm/.claude/skills/orbit-brief/SKILL.md && grep -q "Drifting" orbit-pm/.claude/skills/orbit-brief/SKILL.md && grep -q "Display in conversation" orbit-pm/.claude/skills/orbit-brief/SKILL.md && grep -q "Decision Velocity" orbit-pm/.claude/skills/orbit-brief/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - orbit-brief SKILL.md contains "stalled decision" in the Drifting section rules or output format
    - orbit-brief SKILL.md Drifting output format example shows both an overdue action item AND a stalled decision entry
    - orbit-brief SKILL.md contains "confirmed" and "no follow-up" in the same section context as Drifting
    - orbit-brief SKILL.md contains "Decision Velocity" in Weekly Brief Output Format
    - orbit-brief SKILL.md contains "Display in conversation" in After Generating section
    - orbit-brief SKILL.md contains "⚠️ Drifting" as first section in Daily Brief Output Format
  </acceptance_criteria>
  <done>orbit-brief SKILL.md passes all BREF-01/02/03 requirements and honors D-01/D-02/D-03 locked decisions. Drifting section now includes both overdue action items and stalled decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and patch orbit-decisions SKILL.md against DCSN-01/02/03/04 + D-06/D-07/D-08</name>
  <files>orbit-pm/.claude/skills/orbit-decisions/SKILL.md</files>
  <read_first>
    - orbit-pm/.claude/skills/orbit-decisions/SKILL.md (current state — verify each requirement)
    - .planning/phases/03-intelligence-skills/03-CONTEXT.md (locked decisions D-06, D-07, D-08)
    - .planning/phases/03-intelligence-skills/03-RESEARCH.md (gap analysis for orbit-decisions)
  </read_first>
  <action>
Systematic verification checklist — read the file, check each item, patch only where gaps exist:

**DCSN-01** (reviews full decision log from `.orbit/decisions/log.md`):
- Verify Context Assembly step 1 reads `decisions/log.md` — present (line 19). **No gap.**

**DCSN-02** (stalled = confirmed but no action items within 7 days):
- Verify "stalled" mode mentions "7 days" — present (line 35). **No gap.**
- Verify D-06 (7-day rule) matches — it does. **No gap.**

**DCSN-03** (contradictions across different meetings):
- Verify "contradictions" mode identifies conflicting decisions — present (line 43-45).
- **GAP per D-08**: The current output format has only three sections: `⚠️ Coordinate Conflicts`, `⏸ Uncharted Coordinates`, `✓ Stable Coordinates`. Reversals are mentioned in the "contradictions" analysis mode (line 44) but share the same output section as contradictions. D-08 requires a DISTINCT `↺ Reversal Detected` callout.

  Apply these changes:

  1. In the Output Format section, split `## ⚠️ Coordinate Conflicts` into two sections. After the existing Coordinate Conflicts section, add a new section:

  ```markdown
  ## ↺ Reversal Detected
  **[Decision topic]**
  - [Earlier date, Meeting]: "[original decision]"
  - [Later date, Meeting]: "[new decision that overturns the above]"
  → Confirm: was this reversal intentional? If yes, update the original decision status in the log.
  ```

  2. In the Section rules, add a new bullet after the Coordinate Conflicts rule:
  ```
  - **"↺ Reversal Detected"**: a later meeting overturned an earlier decision without explicit acknowledgment — distinct from contradictions (which are two active conflicting positions). Ask PM to confirm if the reversal was intentional.
  ```

  3. Update the existing Coordinate Conflicts section rule from:
  ```
  **"⚠️ Coordinate Conflicts"**: explicit contradictions and unacknowledged reversals, cite both conflicting sources
  ```
  to:
  ```
  **"⚠️ Coordinate Conflicts"**: explicit contradictions — two active decisions on the same topic that cannot both be true, cite both conflicting sources
  ```

  4. In the Completion Message section, add `↺` reversal count alongside the existing `⚠️` conflicts and `⏸` uncharted counts:
  ```
    ⚠️ N conflicts found — [brief description]
    ↺ N reversals detected — [brief description]
    ⏸ N uncharted — [brief description]
    ✓ N stable coordinates
  ```

**DCSN-04** (suggest resolution with "lock coordinates" language):
- Verify Resolution Suggestion contains "lock" — present (line 79: "lock the N uncharted coordinates"). **No gap.**
- Verify D-07 exact language — present. **No gap.**

Only apply edits for the D-08 reversal section gap. Do not touch sections that already pass.
  </action>
  <verify>
    <automated>grep -q "↺ Reversal Detected" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && grep -q "Coordinate Conflicts" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && grep -q "7 days" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && grep -q "lock" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && grep -q "reversal.*intentional" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - orbit-decisions SKILL.md contains "↺ Reversal Detected" as a distinct section heading in the Output Format
    - orbit-decisions SKILL.md contains "Coordinate Conflicts" as a SEPARATE section from Reversal Detected
    - orbit-decisions SKILL.md Reversal Detected section asks PM to confirm if reversal was intentional
    - orbit-decisions SKILL.md Section rules has a separate bullet for "↺ Reversal Detected" distinct from Coordinate Conflicts
    - orbit-decisions SKILL.md Completion Message includes both ⚠️ conflicts AND ↺ reversals counts
    - orbit-decisions SKILL.md still contains "7 days" for stall threshold (DCSN-02/D-06 unchanged)
    - orbit-decisions SKILL.md still contains "lock" in Resolution Suggestion (DCSN-04/D-07 unchanged)
  </acceptance_criteria>
  <done>orbit-decisions SKILL.md passes all DCSN-01/02/03/04 requirements and honors D-06/D-07/D-08 locked decisions. Reversals now have a distinct output section separate from contradictions.</done>
</task>

</tasks>

<verification>
Run all BREF and DCSN requirement checks:
```bash
# BREF-01: Drifting section with stalled decisions
grep -q "stalled decision" orbit-pm/.claude/skills/orbit-brief/SKILL.md && echo "BREF-01 PASS" || echo "BREF-01 FAIL"

# BREF-02: Weekly has Decision Velocity, Action Burndown, Cross-Theme Patterns
grep -q "Decision Velocity" orbit-pm/.claude/skills/orbit-brief/SKILL.md && echo "BREF-02a PASS" || echo "BREF-02a FAIL"
grep -q "Action Item Burndown" orbit-pm/.claude/skills/orbit-brief/SKILL.md && echo "BREF-02b PASS" || echo "BREF-02b FAIL"
grep -q "Cross-Theme Patterns" orbit-pm/.claude/skills/orbit-brief/SKILL.md && echo "BREF-02c PASS" || echo "BREF-02c FAIL"

# BREF-03: Written to file AND displayed
grep -q "Display in conversation" orbit-pm/.claude/skills/orbit-brief/SKILL.md && echo "BREF-03 PASS" || echo "BREF-03 FAIL"

# DCSN-01: Reads decisions/log.md
grep -q "decisions/log.md" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && echo "DCSN-01 PASS" || echo "DCSN-01 FAIL"

# DCSN-02: 7-day stall detection
grep -q "7 days" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && echo "DCSN-02 PASS" || echo "DCSN-02 FAIL"

# DCSN-03: Distinct reversal callout
grep -q "↺ Reversal Detected" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && echo "DCSN-03 PASS" || echo "DCSN-03 FAIL"

# DCSN-04: "lock coordinates" resolution
grep -q "lock" orbit-pm/.claude/skills/orbit-decisions/SKILL.md && echo "DCSN-04 PASS" || echo "DCSN-04 FAIL"
```
</verification>

<success_criteria>
All 7 requirement checks (BREF-01/02/03, DCSN-01/02/03/04) return PASS. Both SKILL.md files honor all 8 locked decisions (D-01 through D-08) applicable to their domains.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence-skills/03-01-SUMMARY.md`
</output>
